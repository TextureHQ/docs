---
sidebar_position: 5
---

import React, { useEffect } from 'react';
import { useHistory } from '@docusaurus/router';

export const Redirect = () => {
  const history = useHistory();
  useEffect(() => {
    history.replace('/docs/sources/manufacturers');
  }, [history]);
  return null;
};

# Supported Manufacturers

<Redirect />

This page has moved to [Supported Manufacturers](/docs/sources/manufacturers).
